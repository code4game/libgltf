#pragma once

#include <cstdint>
#include <cstring>
#if defined(LIBGLTF_PLATFORM_ANDROID) || defined(LIBGLTF_PLATFORM_MACOS) || defined(LIBGLTF_PLATFORM_IOS)
#include <string>
#endif
#include <vector>
#include <map>
#include <memory>
