language: cpp

sudo: required

matrix:
  include:
    - os: linux
    - os: osx
      osx_image: xcode8.3

install:
  - echo $LANG
  - echo $LC_ALL

script:
  - set -e
  - mkdir ./build && cd ./build
  - cmake ./../
  - make
  - cd ./../

branches:
  only:
    - master
